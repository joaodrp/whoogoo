<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The window is blue before the first frame, so launch does not flash. -->
    <style name="Theme.Whoogoo" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/ultramarine</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
